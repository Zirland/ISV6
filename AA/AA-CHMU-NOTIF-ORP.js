//!JS
// Verze 69

var omezitNaOrp = 337;

#import "CHMU-NOTIF-ORP";

if (resultText != '') {
    resultText += '\n';
    resultText += 'OPIS GŘ HZS ČR';
}

if (resultText !== 'undefined' && resultText) {
    print(resultText);
}

//!JS
// Verze 50

var omezitNaOrp = 337; 

#import "CHMU-NOTIF-ORP"; 

if (vystupText != '') {
    vystupText += "\n";
    vystupText += "OPIS GŘ HZS ČR";
}

if (resultText !== 'undefined' && resultText) {
    print(resultText);
}

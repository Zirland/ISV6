//!JS
// Verze 61

var omezitNaOrp = 1147;

#import "CHMU-SMS-ORP-LK";

if (vystupText != '') {
    vystupText += '. KOPIS HZS LK';
}

if (vystupText !== 'undefined' && vystupText) {
    print(vystupText);
}
